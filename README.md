# ESP32 HTTP Console

![ESP-IDF Version](https://img.shields.io/badge/ESP--IDF-v5.x-blue?style=for-the-badge&logo=espressif)
![Platform](https://img.shields.io/badge/Platform-ESP32-red?style=for-the-badge&logo=espressif)
![License](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)
![Status](https://img.shields.io/badge/Status-Test-orange?style=for-the-badge)

Прошивка для модулів ESP32, яка дозволяє керувати мережевими запитами, сканувати Wi-Fi та налаштовувати пристрій через термінал UART у реальному часі.

## 🛠 Можливості

* NVS Storage: Зберігання SSID та пароля Wi-Fi у незалежній пам'яті (ПЗУ).

* HTTP Client: Виконання GET та POST запитів до віддалених серверів.

* Wi-Fi Scanner: Пошук доступних мереж з відображенням сили сигналу (RSSI).

* System Info: Моніторинг вільної оперативної пам'яті (Heap) та статусу з'єднання.

* Smart Diagnostics: Детальний вивід помилок підключення (невірний пароль, мережа не знайдена).

* Reset NVS: Повне очищення налаштувань.

## 💻 Команди консолі (CLI)

Після прошивки підключіться до пристрою через PuTTY (115200 baud) та використовуйте наступні команди:


|   Команда	  |                   Опис	                   |       Приклад                     |
|  `scan`	  | Сканує ефір на наявність Wi-Fi мереж       | `scan`                            |
|  `set_wifi` | Зберігає SSID та пароль у ПЗУ	           | `set_wifi MyRouter 12345678`      |
|  `get_wifi` |	Показує збережений SSID	                   | `get_wifi`                        |
|  `restart`  | Програмне перезавантаження пристрою	       | `restart`                         |
|  `http`  	  | Відправляє запит (GET/POST)	               | `http GET http://httpbin.org/get` |
|  `info`     | Стан пам'яті та рівень сигналу Wi-Fi	   | `info`                            |
| `reset_nvs` | Повне очищення налаштувань                 | `reset_nvs`                       |  
| `help`      |	Список усіх доступних команд               | `help`                            |


## 🏗 Як зібрати проєкт (Windows 11)
1. Вимоги: Встановлений  Phyton3.13, Git, ESP-IDF v5.x та VS Code (з встановленим розширенням ESP-IDF).

2. Клонування:
```Bash
git clone <url_вашого_репозиторію>
cd esp32-http-console
```
3. Налаштування:

    * Натисніть `F1` -> `ESP-IDF`: `Set Espressif Device Target` -> `esp32`.

4. Збірка та прошивка:

    * Натисніть іконку ⚙️ (Build) у нижній панелі.

    * Натисніть іконку ⚡ (Flash) для завантаження коду в ESP32.

## 📂 Структура проєкту

```Plaintext
├── main/
│   ├── main.c          # Основна логіка (Консоль, Wi-Fi, HTTP)
│   └── CMakeLists.txt  # Залежності компонентів (console, http, nvs)
├── CMakeLists.txt      # Конфігурація проєкту
└── README.md           # Документація
```
## Tasks List (Список реалізованих завдань)
У цьому проєкті успішно вирішили наступні інженерні задачі:

1. Налаштування середовища: Адаптація коду під ESP-IDF v5.x.

2. Інтерактивність: Створення CLI (Command Line Interface) через UART з підтримкою історії команд.

3. Енергонезалежність: Реалізація системи збереження облікових даних Wi-Fi в NVS-секцію Flash-пам'яті.

4. Мережевий менеджмент:

   * Сканування ефіру для пошуку SSID та аналізу сили сигналу (RSSI).

   * Динамічна ініціалізація Wi-Fi без жорсткої прив'язки до коду (hardcode).

   * Обробка подій (Event Loop) для діагностики помилок підключення.

5. Мережева взаємодія: Створення універсального HTTP-клієнта (GET/POST) з обробкою потокових даних.

6. Системна діагностика: Моніторинг оперативної пам'яті (Heap) та керування життєвим циклом пристрою (restart/reset).

## Block Diagram of the Program (Блок-схема програми)

Ця схема відображає архітектуру та логіку роботи ПЗ:

```
graph TD
    Start((Start app_main)) --> NVS[Init NVS & WiFi Stack]
    NVS --> Console[Init Console & Register Commands]
    Console --> CheckNVS{WiFi in NVS?}
    
    CheckNVS -- No --> Wait[Wait for commands: set_wifi / scan]
    CheckNVS -- Yes --> Connect[Attempt WiFi Connection]
    
    Wait --> Loop[REPL Loop / Command Handler]
    Connect --> Loop
    
    Loop --> cmd_scan[scan: Scan Air & List APs]
    Loop --> cmd_set[set_wifi: Save SSID/Pass to NVS]
    Loop --> cmd_http{http: Is Connected?}
    cmd_http -- Yes --> request[Perform HTTP GET/POST]
    cmd_http -- No --> err_msg[Print: No Network Connection]
    Loop --> cmd_info[info: Show Heap & Signal Strength]
    Loop --> cmd_reset[reset_nvs: Clear Flash]
    Loop --> cmd_restart[restart: Reboot Device]
    
    cmd_scan & cmd_set & request & err_msg & cmd_info & cmd_reset --> Loop
```
